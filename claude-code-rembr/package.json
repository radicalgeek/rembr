{
  "name": "claude-code-rembr",
  "version": "0.1.0",
  "description": "Claude Code plugin: persistent cross-session memory backed by Rembr (dev harness — the plugin itself is dependency-free)",
  "license": "MIT",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.6.1"
  }
}
