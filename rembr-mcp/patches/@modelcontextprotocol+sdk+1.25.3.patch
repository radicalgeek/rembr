diff --git a/node_modules/@modelcontextprotocol/sdk/dist/cjs/server/webStandardStreamableHttp.js b/node_modules/@modelcontextprotocol/sdk/dist/cjs/server/webStandardStreamableHttp.js
index 1bc5843..fb97958 100644
--- a/node_modules/@modelcontextprotocol/sdk/dist/cjs/server/webStandardStreamableHttp.js
+++ b/node_modules/@modelcontextprotocol/sdk/dist/cjs/server/webStandardStreamableHttp.js
@@ -207,8 +207,11 @@ class WebStandardStreamableHTTPServerTransport {
         const encoder = new TextEncoder();
         let streamController;
         // Create a ReadableStream with a controller we can use to push SSE events
+        const self = this;
         const readable = new ReadableStream({
             start: controller => {
+                // Send initial SSE comment to establish the connection and prevent proxy timeouts
+                controller.enqueue(encoder.encode(': connected\n\n'));
                 streamController = controller;
             },
             cancel: () => {
@@ -364,7 +367,11 @@ class WebStandardStreamableHTTPServerTransport {
             // Validate the Accept header
             const acceptHeader = req.headers.get('accept');
             // The client MUST include an Accept header, listing both application/json and text/event-stream as supported content types.
-            if (!acceptHeader?.includes('application/json') || !acceptHeader.includes('text/event-stream')) {
+            // WORKAROUND: VS Code MCP OAuth client doesn't send proper Accept headers after OAuth completion
+            // When enableJsonResponse is true, we only require application/json (not text/event-stream)
+            const requiresJson = !acceptHeader?.includes('application/json');
+            const requiresEventStream = !this._enableJsonResponse && !acceptHeader?.includes('text/event-stream');
+            if (requiresJson || requiresEventStream) {
                 return this.createJsonErrorResponse(406, -32000, 'Not Acceptable: Client must accept both application/json and text/event-stream');
             }
             const ct = req.headers.get('content-type');
diff --git a/node_modules/@modelcontextprotocol/sdk/dist/esm/server/webStandardStreamableHttp.js b/node_modules/@modelcontextprotocol/sdk/dist/esm/server/webStandardStreamableHttp.js
index f87830f..0e51bbb 100644
--- a/node_modules/@modelcontextprotocol/sdk/dist/esm/server/webStandardStreamableHttp.js
+++ b/node_modules/@modelcontextprotocol/sdk/dist/esm/server/webStandardStreamableHttp.js
@@ -207,8 +207,11 @@ export class WebStandardStreamableHTTPServerTransport {
         const encoder = new TextEncoder();
         let streamController;
         // Create a ReadableStream with a controller we can use to push SSE events
+        const self = this;
         const readable = new ReadableStream({
             start: controller => {
+                // Send initial SSE comment to establish the connection and prevent proxy timeouts
+                controller.enqueue(encoder.encode(': connected\n\n'));
                 streamController = controller;
             },
             cancel: () => {
@@ -361,7 +364,11 @@ export class WebStandardStreamableHTTPServerTransport {
             // Validate the Accept header
             const acceptHeader = req.headers.get('accept');
             // The client MUST include an Accept header, listing both application/json and text/event-stream as supported content types.
-            if (!acceptHeader?.includes('application/json') || !acceptHeader.includes('text/event-stream')) {
+            // WORKAROUND: VS Code MCP OAuth client doesn't send proper Accept headers after OAuth completion
+            // When enableJsonResponse is true, we only require application/json (not text/event-stream)
+            const requiresJson = !acceptHeader?.includes('application/json');
+            const requiresEventStream = !this._enableJsonResponse && !acceptHeader?.includes('text/event-stream');
+            if (requiresJson || requiresEventStream) {
                 return this.createJsonErrorResponse(406, -32000, 'Not Acceptable: Client must accept both application/json and text/event-stream');
             }
             const ct = req.headers.get('content-type');
